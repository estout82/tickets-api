

const Controller = require('../helper/controller');
const InventoryLocation = require('../models/InventoryLocation');

const controller = new Controller(InventoryLocation);

module.exports = controller;