
const Controller = require('../helper/controller');
const InventoryAssignment = require('../models/InventoryAssignment');

const controller = new Controller(InventoryAssignment);

module.exports = controller;